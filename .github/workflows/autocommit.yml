name: Auto Commit Changes

on:
  schedule:
    - cron: "0 */4 * * *"  # Every 4 hours (UTC)
  workflow_dispatch:       # Allow manual run

jobs:
  auto-commit:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Make changes
        run: |
          echo "## Update on $(date -u +'%Y-%m-%d %H:%M:%S UTC')" >> example_data.md
          echo "" >> example_data.md
          echo "- Automated update by GitHub Actions" >> example_data.md
          echo "" >> example_data.md

      - name: Check for changes
        id: git-check
        run: |
          git config --global --add safe.directory "$GITHUB_WORKSPACE"
          if git diff --quiet; then
            echo "changed=false" >> $GITHUB_OUTPUT
          else
            echo "changed=true" >> $GITHUB_OUTPUT
          fi

      - name: Commit changes
        if: steps.git-check.outputs.changed == 'true'
        uses: stefanzweifel/git-auto-commit-action@v2.3.0
        with:
          commit_message: "Auto update: $(date -u +'%Y-%m-%d %H:%M:%S UTC')"
          branch: ${{ github.ref_name }}
          commit_options: ''
          file_pattern: 'example_data.md'
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
